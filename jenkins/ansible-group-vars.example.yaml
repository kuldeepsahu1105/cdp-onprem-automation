# Optional extra overrides (paste into Jenkins ANSIBLE_GROUP_VARS_YAML text area).
# Merged on top of ansible-playbooks/group_vars/all.yml at Ansible runtime.
#
# Common per-deployment tweaks:
# ipaserver_domain: cldrsetup.local
# cm_version: "7.13.1.0"
# cdh_version: "7.3.2.10000"
# cm_repo_source: public
# cm_debian_use_ubuntu_repo: true
# ecs_deploy_on_arm64: false
# target_cpu_architecture: arm64
