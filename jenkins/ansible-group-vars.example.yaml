# Optional extra overrides (paste into Jenkins ANSIBLE_GROUP_VARS_YAML or use as a file)
# Merged on top of group_vars/all.yml and individual Jenkins Ansible parameters.
#
# Common per-deployment tweaks:
# cm_debian_use_ubuntu_repo: true
# cm_debian_ubuntu_compat_version: "2204"
# ecs_deploy_on_arm64: false
# target_cpu_architecture: arm64
# cm_ldap_enabled: true
# deployment_environment: aws
