collections:
  - name: community.general
  - name: community.postgresql
  - name: ansible.posix
  - name: community.crypto
  - name: freeipa.ansible_freeipa
  - name: git+https://github.com/cloudera-labs/cloudera.cluster.git,v4.4.0
  # Private-cloud CDW/CDE/CAI modules (private_cloud, cpu_requests, ml resource_pool)
  - name: git+https://github.com/cloudera-labs/cloudera.cloud.git,main
