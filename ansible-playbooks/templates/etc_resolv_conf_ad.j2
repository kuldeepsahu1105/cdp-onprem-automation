# Generated by Ansible ({{ effective_deployment_env | default('unknown') }})
{% if dns_search_domains | default([]) | length > 0 %}
search {{ dns_search_domains | join(' ') }}
{% endif %}
{% for ns in dns_nameservers | default([]) %}
nameserver {{ ns }}
{% endfor %}
