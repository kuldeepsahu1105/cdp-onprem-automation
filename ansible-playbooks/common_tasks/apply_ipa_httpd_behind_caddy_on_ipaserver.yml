---
# Deprecated alias — use sync_ipa_httpd_caddy_proxy_state_on_ipaserver.yml (playbook 16).

- name: Sync ipaserver HTTPD Caddy proxy state (apply or remove per flag)
  ansible.builtin.import_tasks: sync_ipa_httpd_caddy_proxy_state_on_ipaserver.yml
