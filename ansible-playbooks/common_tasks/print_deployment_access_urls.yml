---
# Emit access URLs to Ansible logs (grep CDP_ACCESS_URLS in phase logs).

- name: Print CDP access URLs for pipeline log and email collectors
  ansible.builtin.debug:
    msg: "{{ lookup('template', 'deployment_access_urls_report.j2') }}"
  changed_when: false
